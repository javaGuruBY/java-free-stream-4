Объект - Кот
  	состояние:
              голодный/сытый;
	      лежит;
  	поведение:
              покормить (изменяется уровень сытости);
              играть(изменяется положение в пространстве).

Объект - Стикеры
  	состояние:
              разноцветные;
              широкие/узкие;
              бумажные;
  	поведение:
              делать пометки (стикеры перестают быть пустыми);
              клеить (количество уменьшается).

Объект - Календарь
  	состояние:
              настенный/настольный/перекидной;
              текущий/прошлый год;
  	поведение:
              повесить/поставить (меняется положение в пространстве);
              перелистнуть на следующий день (меняется состояние).

Объект - Телефон
	  состояние:
              производитель;
              модель;
              смартфон/телефон/бабушкофон;
	      разряженный;
              в кармане;
  	поведение:
              заряжать (меняется уровень заряда);
              звонить.

Объект - Часы
  	состояние:
              наручные/настенные/настольные;
              механические/кварцевые;
              точность хода;
  	поведение:
	      надеть/повесить/поставить;
              настроить время.
              
Объект - Монитор
  	состояние:
	      включен/выключен;
              рабочий/бракованный;
              новый/б.у.;
  	поведение:
              включить/выключить;
              работать за ним (меняется изображение на экране).

Объект - Горшок с цветком
	  состояние:
              элемент интерьера;
              стоит на подоконнике;
	  поведение:
              поливать (цветок продолжает жить).

Объект - Маршрутка
  	состояние:
              полная/пустая;
              марка авто;
              маршрут следования;
  	поведение:
              ехать;
              оплачивать проезд (казна маршрутки растет).

Объект - Ножницы
  	состояние:
              острые/тупые;
              кухонные/для маникюра;
  	поведение:
              резать;
              заточить.

Объект - Шкаф
	  состояние:
              встраиваемый;
              материал дверей;
              тип шкафа (купе/обычный);
  	поведение:
              вешать одежду;
              открыть/закрыть.
